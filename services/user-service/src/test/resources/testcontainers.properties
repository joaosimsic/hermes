docker.client.strategy=org.testcontainers.dockerclient.UnixSocketClientProviderStrategy
# Container reuse is controlled by ~/.testcontainers.properties (local dev optimization)
# In CI, containers are always created fresh
