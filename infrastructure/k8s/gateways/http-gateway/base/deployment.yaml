apiVersion: apps/v1
kind: Deployment
metadata:
  name: http-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: http-gateway
  template:
    metadata:
      labels:
        app: http-gateway
      annotations:
        prometheus.io/scrape: "true"           
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8080"
    spec:
      initContainers:
      - name: wait-for-redis
        image: redis:7-alpine
        command:
        - sh
        - -c
        - |
          until redis-cli -h gateway-cache -p 6379 ping | grep -q PONG; do
            echo "Waiting for Redis to be ready..."
            sleep 2
          done
          echo "Redis is ready!"
      - name: wait-for-keycloak
        image: curlimages/curl:8.5.0
        command:
        - sh
        - -c
        - |
            until curl -s -f -o /dev/null http://keycloak:8180/realms/hermes; do
              echo "Waiting for Keycloak to respond at http://keycloak:8180/realms/hermes..."
              sleep 5
            done
            echo "Keycloak is ready!"
      - name: wait-for-rabbitmq
        image: curlimages/curl:8.5.0
        command:
        - sh
        - -c
        - |
          until curl -f http://rabbitmq:15672/api/healthchecks/node -u ${RABBITMQ_USER}:${RABBITMQ_PASSWORD}; do
            echo "Waiting for RabbitMQ to be ready..."
            sleep 2
          done
          echo "RabbitMQ is ready!"
        env:
        - name: RABBITMQ_USER
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secrets
              key: RABBITMQ_USERNAME
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secrets
              key: RABBITMQ_PASSWORD
      containers:
        - name: http-gateway
          image: http-gateway:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: gateway-config
            - secretRef:
                name: gateway-secrets
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 30
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
