apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
labels:
- pairs:
    environment: staging
  includeSelectors: true
patches:
- patch: |-
    - op: replace
      path: /spec/replicas
      value: 2
  target:
    kind: StatefulSet
    name: scylladb
