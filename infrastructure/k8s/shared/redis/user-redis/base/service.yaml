apiVersion: v1
kind: Service
metadata:
  name: user-redis
  labels:
    app: user-redis
    component: cache
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: user-redis
