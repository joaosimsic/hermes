apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-redis-data
  labels:
    app: user-redis
    component: cache
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
