apiVersion: v1
kind: Service
metadata:
  name: gateway-redis
  labels:
    app: gateway-redis
    component: cache
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: gateway-redis
