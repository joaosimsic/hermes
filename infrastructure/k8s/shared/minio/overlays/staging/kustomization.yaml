apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
secretGenerator:
- name: minio-secrets
  envs:
  - secrets.env
labels:
- pairs:
    environment: staging
  includeSelectors: true
