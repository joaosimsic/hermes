apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- deployment.yaml
- service.yaml
- pvc.yaml
- init-job.yaml
labels:
- pairs:
    app: minio
  includeSelectors: true
- pairs:
    managed-by: kustomize
  includeSelectors: true
