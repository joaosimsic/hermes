apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    io.kompose.service: keycloak
spec:
  ports:
    - name: "8180"
      port: 8180
      targetPort: 8180
  selector:
    io.kompose.service: keycloak
