{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.io/joaosimsic/schemas/events/user-deleted.v1.schema.json",
  "title": "User Deleted Event",
  "description": "Event published when a user is deleted.",
  "type": "object",
  "properties": {
    "aggregateId": {
      "type": "integer",
      "format": "int64",
      "description": "The unique identifier of the deleted user."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the user was deleted."
    },
    "eventType": {
      "type": "string",
      "default": "USER_DELETED"
    }
  },
  "required": ["aggregateId", "occurredAt", "eventType"],
  "additionalProperties": false
}
