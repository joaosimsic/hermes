{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.io/joaosimsic/schemas/events/user-updated.v1.schema.json",
  "title": "User Updated Event",
  "description": "Event published when a user is updated.",
  "type": "object",
  "properties": {
    "aggregateId": {
      "type": "integer",
      "format": "int64",
      "description": "The unique identifier of the updated user."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "The email address of the updated user."
    },
    "name": {
      "type": "string",
      "description": "The name of the updated user."
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "The timestamp when the user was updated."
    },
    "eventType": {
      "type": "string",
      "default": "USER_UPDATED"
    }
  },
  "required": ["aggregateId", "email", "name", "occurredAt", "eventType"],
  "additionalProperties": false
}
